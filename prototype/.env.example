# Copy to .env and fill in. .env is gitignored and never reaches the browser —
# server.ts attaches the Authorization header on the Deno side.

TYPESAFE_API_KEY=

# Optional overrides
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# PORT=8787
